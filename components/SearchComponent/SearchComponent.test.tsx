/*import * as enzyme from 'enzyme';
// import {} from 'jasmine';
import * as React from 'react';
import SearchComponent from './SearchComponent';

describe('dateFormat function', () => {
    it('Changes the date to the correct format'), () => {
        const searchcomponent = enzyme.shallow(<SearchComponent vacationType = 'zuipen'/>);
        expect(searchcomponent.instance().dateFormat('1996-1-28')).toEqual('20-11-1996')
        //dateFormat('1996-11-20')).toEqual('20-11-1996')
    };
});
*/
