/*import * as React from 'react';
import '../../../styles/index.less';
import { GoogleMap, Marker } from 'react-google-maps';
// Defining the interface.
export interface MapProps {spacerHeight: string; }

export default class Map extends React.Component<MapProps> {

// Defining the default props.
public static defaultProps: Partial<MapProps> = {
    isMarkerShown = true;

  };

// Declaring what is being rendered.
  render() {
    return (
      <GoogleMap
  defaultZoom={8}
  defaultCenter={{ lat: -34.397, lng: 150.644 }}
>
  {MapProps.isMarkerShown && <Marker position={{ lat: -34.397, lng: 150.644 }} />}
</GoogleMap>
    );
  }
}
*/
