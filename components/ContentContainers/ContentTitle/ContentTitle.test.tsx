/*import * as enzyme from 'enzyme';
import * as React from 'react';
import ContentTitle from './ContentTitle';

it('renders the correct title of the page'), () => {
    const ContentTitle = enzyme.shallow(<ct-title-container TitleContent = 'zuipvakantie'/>);
    expect(ContentTitle.find('.TitleContent').text()).toEqual('zuipvakantie')
});
*/
